# 订单评价功能测试指南

## 功能概述
订单评价功能已完整实现，包括后端API、数据库、前端页面和路由配置。

## 测试步骤

### 1. 数据库初始化（如果需要）
首先执行数据库脚本创建评价表：
```sql
-- 执行评价功能数据库脚本.sql
```

### 2. 前端页面检查
确认以下文件已正确配置：

#### 路由配置 (app.json)
- ✅ 已添加 `pages/orderComment/orderComment` 到路由列表

#### API调用 (api/order/index.js)
- ✅ 已添加 `submitOrderComment` - 提交评价
- ✅ 已添加 `getOrderComment` - 获取评价信息
- ✅ 已添加 `getDriverRating` - 获取司机评分

#### 评价页面 (pages/orderComment/)
- ✅ WXML - 页面结构完整
- ✅ JS - 逻辑实现完整
- ✅ JSON - 配置正确

#### 订单详情页 (pages/orderDetail/)
- ✅ 已添加评价按钮
- ✅ 已集成评价检查逻辑

### 3. 功能测试流程

#### 步骤1：创建并完成订单
1. 创建一个新订单
2. 让司机接单
3. 完成行程（订单状态变为COMPLETED）

#### 步骤2：进入订单详情页
1. 在订单列表中找到已完成的订单
2. 点击进入订单详情页

#### 步骤3：验证评价入口
1. 确认能看到"评价订单"按钮
2. 确认如果没有评价，按钮会显示
3. 如果已经评价，按钮不应显示

#### 步骤4：进行评价
1. 点击"评价订单"按钮
2. 进入评价页面
3. 选择星级评分（1-5星）
4. 填写评价内容（选填）
5. 点击"提交评价"

#### 步骤5：验证评价结果
1. 提交成功后自动返回订单详情页
2. 订单详情页不再显示评价按钮
3. 可以查看已提交的评价

### 4. API测试

#### 提交评价
```http
POST /order/comment/submit
Content-Type: application/json

{
  "orderId": 1,
  "driverId": 1001,
  "customerId": 2001,
  "rate": 5,
  "remark": "服务很好！"
}
```

#### 获取评价信息
```http
GET /order/comment/get/1
```

#### 获取司机评分
```http
GET /order/comment/driver/rating/1001
```

## 预期行为

### 评价前
- 订单详情页显示"评价订单"按钮
- 按钮只在订单已完成且未评价时显示

### 评价中
- 评价页面显示星级评分组件
- 可以选择1-5星
- 可以输入评价内容（最多200字符）
- 提交按钮需要先选择评分才能点击

### 评价后
- 订单详情页不再显示评价按钮
- 可以查看已提交的评价信息
- 司机的评分统计会更新

## 注意事项

1. **订单状态验证**：只有状态为COMPLETED或PAID的订单才能评价
2. **重复评价防护**：一个订单只能评价一次
3. **数据完整性**：评价后会自动关联订单ID和用户信息
4. **前端适配**：评价页面使用了tmui组件库，确保样式一致

## 故障排查

### 评价按钮不显示
- 检查订单状态是否为COMPLETED
- 检查是否已经评价过
- 检查WXML模板是否正确绑定变量

### 提交评价失败
- 检查网络连接
- 检查API调用是否正确
- 检查后端日志是否有错误

### 评价后数据不更新
- 检查数据库是否成功插入数据
- 检查前端是否正确刷新页面
- 检查后端是否正确返回数据