# 财务管理功能说明文档

## 一、功能概述

财务管理模块包含三大核心功能：

| 功能 | 说明 |
|------|------|
| **财务统计概览** | 展示今日/本周/本月的营收金额、订单量、提现金额 |
| **订单报表** | 展示所有订单的详细信息，包含账单费用明细和分账信息 |
| **提现记录** | 展示所有司机的提现记录，包含司机信息和账户余额 |

---

## 二、模块结构

### 2.1 后端模块层级

```
web-mgr (管理端控制器)
    ↓ 注入 FinanceFeignClient
service-driver-client (Feign客户端)
    ↓ 注入 FinanceService
service-driver (真实业务实现)
    ↓ 联表查询
service-order (订单服务，OrderInfoFeignClient)
```

### 2.2 涉及的数据库表

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `driver_account_detail` | 司机账户明细表 | driver_id, trade_type, amount, trade_no, content, create_time |
| `driver_account` | 司机账户表 | driver_id, available_amount |
| `driver_info` | 司机信息表 | id, name, phone |
| `order_info` | 订单表 | id, order_no, customer_id, driver_id, status, start_location, end_location, create_time |
| `order_bill` | 订单账单表 | order_id, pay_amount, distance_fee, wait_fee, toll_fee, parking_fee, other_fee, coupon_amount |
| `order_profitsharing` | 订单分账表 | order_id, platform_income, driver_income |

---

## 三、接口路径

### 3.1 管理端接口 (web-mgr)

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取财务统计 | GET | `/mgr/finance/statistics` | 返回统计数据 |
| 获取订单报表 | POST | `/mgr/finance/order/report/{page}/{limit}` | 分页查询订单报表 |
| 获取提现记录 | POST | `/mgr/finance/withdraw/report/{page}/{limit}` | 分页查询提现记录 |

### 3.2 内部接口 (service-driver)

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取财务统计 | GET | `/driver/finance/statistics` | 返回统计数据 |
| 获取订单报表 | POST | `/driver/finance/order/report/{page}/{limit}` | 分页查询订单报表 |
| 获取提现记录 | POST | `/driver/finance/withdraw/report/{page}/{limit}` | 分页查询提现记录 |

---

## 四、接口详解

### 4.1 获取财务统计数据

**请求路径**: `GET /mgr/finance/statistics`

**请求参数**: 无

**返回数据** (`FinanceStatisticsVo`):

| 字段 | 类型 | 说明 |
|------|------|------|
| todayIncome | BigDecimal | 今日营收 |
| weekIncome | BigDecimal | 本周营收 |
| monthIncome | BigDecimal | 本月营收 |
| todayOrderCount | Long | 今日订单量 |
| weekOrderCount | Long | 本周订单量 |
| monthOrderCount | Long | 本月订单量 |
| todayWithdraw | BigDecimal | 今日提现金额 |
| weekWithdraw | BigDecimal | 本周提现金额 |
| monthWithdraw | BigDecimal | 本月提现金额 |

**数据来源**:

1. **订单量统计**:
   - 调用 `OrderInfoFeignClient.getOrderNumByTime(startTime, endTime)`
   - 内部调用 `service-order` 的 `OrderInfoService.getOrderNumByTime()`

2. **提现金额统计**:
   - 直接查询 `driver_account_detail` 表
   - 筛选条件: `trade_type = '1203'` (提现)
   - 时间范围: 今日/本周/本月

**实现类**: `FinanceServiceImpl.getStatistics()`

---

### 4.2 获取订单报表

**请求路径**: `POST /mgr/finance/order/report/{page}/{limit}`

**路径参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| page | Long | 当前页码 |
| limit | Long | 每页记录数 |

**请求体** (`OrderReportQueryForm` / `OrderQueryForm`):

| 字段 | 类型 | 说明 |
|------|------|------|
| orderNo | String | 订单号(模糊查询) |
| customerId | Long | 乘客ID |
| driverId | Long | 司机ID |
| status | Integer | 订单状态 |
| startDate | String | 开始日期 |
| endDate | String | 结束日期 |

**返回数据** (`PageVo<OrderReportVo>`):

| 字段 | 类型 | 说明 |
|------|------|------|
| records | List<OrderReportVo> | 订单报表列表 |
| total | Long | 总记录数 |
| pages | Long | 总页数 |

**OrderReportVo 详细字段**:

| 字段 | 类型 | 说明 | 数据来源 |
|------|------|------|----------|
| id | Long | 订单ID | order_info.id |
| orderNo | String | 订单号 | order_info.order_no |
| customerId | Long | 乘客ID | order_info.customer_id |
| driverId | Long | 司机ID | order_info.driver_id |
| startLocation | String | 上车地点 | order_info.start_location |
| endLocation | String | 下车地点 | order_info.end_location |
| payAmount | BigDecimal | 订单金额 | order_bill.pay_amount |
| distanceFee | BigDecimal | 里程费 | order_bill.distance_fee |
| waitFee | BigDecimal | 等时费 | order_bill.wait_fee |
| tollFee | BigDecimal | 路桥费 | order_bill.toll_fee |
| parkingFee | BigDecimal | 停车费 | order_bill.parking_fee |
| otherFee | BigDecimal | 其他费用 | order_bill.other_fee |
| couponAmount | BigDecimal | 优惠券金额 | order_bill.coupon_amount |
| platformIncome | BigDecimal | 平台收入 | order_profitsharing.platform_income |
| driverIncome | BigDecimal | 司机收入 | order_profitsharing.driver_income |
| status | Integer | 订单状态 | order_info.status |
| statusName | String | 订单状态名称 | 枚举: 1=待接单, 2=已接单, 3=司机已到达, 5=服务中, 7=待支付, 8=已支付, -1=已取消 |
| createTime | String | 创建时间 | order_info.create_time |

**联表查询说明**:

1. **主查询**: `order_info` 表，按创建时间降序
2. **联表查询订单账单**: 通过 `order_id` 关联 `order_bill` 表
3. **联表查询分账信息**: 通过 `order_id` 关联 `order_profitsharing` 表

**实现类**: `FinanceServiceImpl.getOrderReportPage()`

---

### 4.3 获取提现记录

**请求路径**: `POST /mgr/finance/withdraw/report/{page}/{limit}`

**路径参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| page | Long | 当前页码 |
| limit | Long | 每页记录数 |

**请求体** (`WithdrawReportQueryForm`):

| 字段 | 类型 | 说明 |
|------|------|------|
| tradeNo | String | 交易号(精确查询) |
| driverId | Long | 司机ID |
| startDate | String | 开始日期 |
| endDate | String | 结束日期 |

**返回数据** (`PageVo<WithdrawReportVo>`):

| 字段 | 类型 | 说明 |
|------|------|------|
| records | List<WithdrawReportVo> | 提现记录列表 |
| total | Long | 总记录数 |
| pages | Long | 总页数 |

**WithdrawReportVo 详细字段**:

| 字段 | 类型 | 说明 | 数据来源 |
|------|------|------|----------|
| id | Long | 记录ID | driver_account_detail.id |
| tradeNo | String | 交易号 | driver_account_detail.trade_no |
| driverId | Long | 司机ID | driver_account_detail.driver_id |
| driverName | String | 司机姓名 | driver_info.name |
| driverPhone | String | 司机手机 | driver_info.phone |
| tradeTypeName | String | 交易类型 | 固定值: "提现" |
| amount | BigDecimal | 提现金额 | driver_account_detail.amount |
| availableAmount | BigDecimal | 账户余额 | driver_account.available_amount |
| content | String | 交易说明 | driver_account_detail.content |
| createTime | String | 交易时间 | driver_account_detail.create_time |

**联表查询说明**:

1. **主查询**: `driver_account_detail` 表
   - 筛选条件: `trade_type = '1203'` (提现)
   - 排序: 按创建时间降序

2. **批量联表查询司机信息**: 通过 `driver_id` 关联 `driver_info` 表
   - 提取所有涉及的 `driver_id`，批量查询 `driver_info`
   - 使用 `Map<Long, DriverInfo>` 缓存结果

3. **批量联表查询账户余额**: 通过 `driver_id` 关联 `driver_account` 表
   - 提取所有涉及的 `driver_id`，批量查询 `driver_account`
   - 使用 `Map<Long, DriverAccount>` 缓存结果

**实现类**: `FinanceServiceImpl.getWithdrawReportPage()`

---

## 五、文件清单

### 5.1 Model层 (VO和表单)

| 文件路径 | 说明 |
|----------|------|
| `model/.../vo/finance/FinanceStatisticsVo.java` | 财务统计VO |
| `model/.../vo/finance/OrderReportVo.java` | 订单报表VO |
| `model/.../vo/finance/WithdrawReportVo.java` | 提现记录VO |
| `model/.../form/order/OrderQueryForm.java` | 订单查询表单(新增startDate/endDate字段) |

### 5.2 Service层

| 文件路径 | 说明 |
|----------|------|
| `service-driver/.../service/FinanceService.java` | 财务服务接口 |
| `service-driver/.../service/impl/FinanceServiceImpl.java` | 财务服务实现 |
| `service-driver/.../controller/FinanceController.java` | 内部控制器 |

### 5.3 FeignClient

| 文件路径 | 说明 |
|----------|------|
| `service-driver-client/.../driver/client/FinanceFeignClient.java` | 财务Feign客户端 |

### 5.4 管理端

| 文件路径 | 说明 |
|----------|------|
| `web-mgr/.../controller/FinanceController.java` | 管理端控制器 |

### 5.5 前端

| 文件路径 | 说明 |
|----------|------|
| `mgr-front/src/api/finance.js` | 财务API调用 |
| `mgr-front/src/views/finance/FinanceList.vue` | 财务管理页面 |
| `mgr-front/src/router/index.js` | 路由配置 |
| `mgr-front/src/layouts/MainLayout.vue` | 侧边栏菜单 |

---

## 六、前端页面结构

### 6.1 页面布局

```
┌─────────────────────────────────────────────────────┐
│  财务管理                                             │
├─────────────────────────────────────────────────────┤
│ ┌───────────┐ ┌───────────┐ ┌───────────┐          │
│ │ 今日营收   │ │ 本周营收   │ │ 本月营收   │          │
│ │ ¥0.00    │ │ ¥0.00    │ │ ¥0.00    │          │
│ └───────────┘ └───────────┘ └───────────┘          │
│ ┌───────────┐ ┌───────────┐ ┌───────────┐          │
│ │ 今日订单   │ │ 本周订单   │ │ 本月订单   │          │
│ │   0      │ │   0      │ │   0      │          │
│ └───────────┘ └───────────┘ └───────────┘          │
│ ┌───────────┐ ┌───────────┐ ┌───────────┐          │
│ │ 今日提现   │ │ 本周提现   │ │ 本月提现   │          │
│ │ ¥0.00    │ │ ¥0.00    │ │ ¥0.00    │          │
│ └───────────┘ └───────────┘ └───────────┘          │
├─────────────────────────────────────────────────────┤
│ [订单报表] [提现记录]                                 │
├─────────────────────────────────────────────────────┤
│ 筛选条件区                                            │
│ ─────────────────────────────────────────          │
│ 订单列表/提现列表表格...                              │
└─────────────────────────────────────────────────────┘
```

### 6.2 订单报表筛选条件

- 开始日期 (日期选择器)
- 结束日期 (日期选择器)
- 订单号 (输入框)
- 订单状态 (下拉选择: 待接单/已接单/司机已到达/服务中/待支付/已支付/已取消)

### 6.3 提现记录筛选条件

- 开始日期 (日期选择器)
- 结束日期 (日期选择器)
- 司机ID (输入框)
- 交易号 (输入框)

### 6.4 订单报表展示列

| 列名 | 说明 |
|------|------|
| 序号 | 索引 |
| 订单号 | 订单唯一编号 |
| 乘客ID | 下单乘客 |
| 司机ID | 接单司机 |
| 上车地点 | 起点位置 |
| 下车地点 | 终点位置 |
| 订单金额 | 实付金额 |
| 里程费 | 行驶里程费用 |
| 等时费 | 等待时间费用 |
| 路桥费 | 过桥过路费用 |
| 停车费 | 停车费用 |
| 其他费用 | 额外费用 |
| 优惠券 | 优惠减免金额 |
| 平台收入 | 平台分账所得(绿色) |
| 司机收入 | 司机分账所得(蓝色) |
| 状态 | 订单状态标签 |
| 创建时间 | 订单创建时间 |

### 6.5 提现记录展示列

| 列名 | 说明 |
|------|------|
| 序号 | 索引 |
| 交易号 | 唯一交易编号 |
| 司机ID | 提现司机 |
| 司机姓名 | 便于辨认 |
| 司机手机 | 便于辨认 |
| 交易类型 | 固定显示"提现" |
| 提现金额 | 提现金额(红色) |
| 账户余额 | 提现后可用余额 |
| 交易说明 | 交易备注 |
| 交易时间 | 提现时间 |

---

## 七、关键实现逻辑

### 7.1 时间范围计算

```java
// 今日开始: 2024-01-01 00:00:00
private String getTodayStart() {
    return LocalDate.now().toString() + " 00:00:00";
}

// 今日结束: 2024-01-01 23:59:59
private String getTodayEnd() {
    return LocalDate.now().toString() + " 23:59:59";
}

// 本周开始: 2023-12-25 00:00:00
private String getWeekStart() {
    return LocalDate.now().minusWeeks(1).toString() + " 00:00:00";
}

// 本月开始: 2024-01-01 00:00:00
private String getMonthStart() {
    return LocalDate.now().withDayOfMonth(1).toString() + " 00:00:00";
}
```

### 7.2 订单报表 - 批量联表查询优化

```java
// 1. 先查询订单主表
PageVo<OrderInfo> orderPage = orderInfoFeignClient
    .findOrderInfoPageByCondition(page, limit, queryForm)
    .getData();

// 2. 循环每个订单，分别查询账单和分账信息
for (OrderInfo order : orderPage.getRecords()) {
    // 查询账单
    OrderBillVo billVo = orderInfoFeignClient
        .getOrderBillInfo(order.getId()).getData();

    // 查询分账
    OrderProfitsharingVo profitsharingVo = orderInfoFeignClient
        .getOrderProfitsharing(order.getId()).getData();

    // 组装 OrderReportVo
}
```

### 7.3 提现记录 - 批量查询优化

```java
// 1. 查询提现明细
Page<DriverAccountDetail> detailPage = driverAccountDetailMapper
    .selectPage(pageParam, wrapper);

// 2. 提取所有涉及的司机ID
List<Long> driverIds = detailPage.getRecords().stream()
    .map(DriverAccountDetail::getDriverId)
    .distinct()
    .collect(Collectors.toList());

// 3. 批量查询司机信息
Map<Long, DriverInfo> driverInfoMap = ...
List<DriverInfo> driverInfos = driverInfoMapper.selectBatchIds(driverIds);
driverInfoMap = driverInfos.stream()
    .collect(Collectors.toMap(DriverInfo::getId, d -> d));

// 4. 批量查询账户余额
Map<Long, DriverAccount> driverAccountMap = ...
List<DriverAccount> accounts = driverAccountMapper.selectBatchIds(driverIds);
driverAccountMap = accounts.stream()
    .collect(Collectors.toMap(DriverAccount::getId, a -> a));

// 5. 组装结果时从Map中获取
DriverInfo driverInfo = driverInfoMap.get(detail.getDriverId());
DriverAccount account = driverAccountMap.get(detail.getDriverId());
```

### 7.4 订单状态枚举

| 状态值 | 状态名称 |
|--------|----------|
| 1 | 待接单 |
| 2 | 已接单 |
| 3 | 司机已到达 |
| 5 | 服务中 |
| 7 | 待支付 |
| 8 | 已支付 |
| -1 | 已取消 |

### 7.5 交易类型枚举

| 类型值 | 类型名称 |
|--------|----------|
| 1201 | 进账 |
| 1202 | 解锁 |
| 1203 | 提现 |
| 1204 | 系统奖励 |

---

## 八、注意事项

### 8.1 Feign调用返回值处理

所有Feign调用的返回值都是 `Result<T>` 包装类型，需要调用 `.getData()` 获取实际数据:

```java
// 错误写法
PageVo<OrderInfo> orderPage = orderInfoFeignClient.findOrderInfoPageByCondition(...);

// 正确写法
PageVo<OrderInfo> orderPage = orderInfoFeignClient.findOrderInfoPageByCondition(...).getData();
```

### 8.2 Lambda表达式中的变量

在MyBatis-Plus的LambdaQueryWrapper中使用lambda时，被捕获的变量必须是final或effectively final:

```java
// 错误写法 - endDate在lambda表达式中被修改
wrapper.le(DriverAccountDetail::getCreateTime, endDate + " 23:59:59");

// 正确写法 - 先计算出最终值并赋值给final变量
final String endDateFull = (endDate != null && StringUtils.hasText(endDate)) ? endDate + " 23:59:59" : null;
if (endDateFull != null) {
    wrapper.le(DriverAccountDetail::getCreateTime, endDateFull);
}
```

### 8.3 日期范围筛选

查询条件中的结束日期需要补全时间:

```java
// 原始结束日期: 2024-01-01
// 查询结束日期: 2024-01-01 23:59:59
wrapper.le(OrderInfo::getCreateTime, endDate + " 23:59:59");
```

---

## 九、后续可扩展功能

1. **营收统计完善**: 当前营收金额暂时返回0，可通过聚合查询 `order_bill` 表的 `pay_amount` 字段获得
2. **数据导出**: 可添加Excel导出功能
3. **对账功能**: 可添加订单支付状态 vs 分账状态的一致性核对
